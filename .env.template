# mongo
MONGO_INITDB_ROOT_USERNAME=<user>
MONGO_INITDB_ROOT_PASSWORD=<password>
MONGO_DB=<db>
MONGO_USER=<user>
MONGO_PASSWORD=<password>
MONGO_PORT=<port>

# express
EXPRESS_NODE_ENV=<env>
EXPRESS_PORT=<port>
EXPRESS_RATE_LIMIT_WINDOW_MS=<rate_limit>
EXPRESS_RATE_LIMIT_MAX=<rate_max>

# vite
VITE_PORT=<port>

# nginx
NGINX_HOST=<host>
NGINX_HOST_PORT=<port>
NGINX_PORT=<port>
